---
import Layout from '../layouts/Layout.astro';
---

<Layout title="NetLife - Time Management">
  <main>
    <div class="container">
      <h1>NetLife</h1>
      <p class="subtitle">Visualize how you spend your time</p>
      
      <div class="control-message" id="controlMessage">
        You have <span class="percentage">100%</span> of your life in your control
      </div>

      <div class="content-wrapper">
        <div class="chart-section">
          <canvas id="pieChart" width="400" height="400"></canvas>
        </div>

        <div class="entries-section">
          <div class="entries-header">
            <h2>Time Entries</h2>
            <button id="addEntryBtn" class="add-btn" aria-label="Add time entry">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="12" y1="5" x2="12" y2="19"></line>
                <line x1="5" y1="12" x2="19" y2="12"></line>
              </svg>
            </button>
          </div>

          <div id="entriesList" class="entries-list"></div>

          <div id="entryForm" class="entry-form hidden">
            <h3>Add Time Entry</h3>
            <div class="form-group">
              <label for="frequency">Frequency</label>
              <select id="frequency" required>
                <option value="daily">Daily</option>
                <option value="weekdays">Weekdays (Mon-Fri)</option>
                <option value="weekends">Weekends (Sat-Sun)</option>
                <option value="weekly">Weekly</option>
                <option value="monthly">Monthly</option>
                <option value="yearly">Yearly</option>
              </select>
            </div>

            <div class="form-group">
              <label for="description">Description</label>
              <input type="text" id="description" placeholder="e.g., Work, Sleep, Exercise" required />
            </div>

            <div class="form-group">
              <label for="hours">Hours per occurrence</label>
              <input type="number" id="hours" min="0" max="24" step="0.5" placeholder="e.g., 8" required />
            </div>

            <div class="form-actions">
              <button id="saveBtn" class="btn-primary">Save</button>
              <button id="cancelBtn" class="btn-secondary">Cancel</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</Layout>

<style>
  main {
    max-width: 1400px;
    margin: 0 auto;
  }

  .container {
    background: white;
    border-radius: 20px;
    padding: 2rem;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  }

  h1 {
    font-size: 3rem;
    font-weight: 700;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
    text-align: center;
  }

  .subtitle {
    text-align: center;
    color: #666;
    font-size: 1.1rem;
    margin-bottom: 2rem;
  }

  .control-message {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 1.5rem;
    border-radius: 12px;
    text-align: center;
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 2rem;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
  }

  .percentage {
    font-size: 1.6rem;
    font-weight: 700;
  }

  .content-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: start;
  }

  .chart-section {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 2rem;
    background: #f8f9fa;
    border-radius: 12px;
  }

  #pieChart {
    max-width: 100%;
    height: auto;
  }

  .entries-section {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .entries-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .entries-header h2 {
    font-size: 1.8rem;
    color: #333;
  }

  .add-btn {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 50%;
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
  }

  .add-btn:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
  }

  .add-btn:active {
    transform: scale(0.95);
  }

  .entries-list {
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    max-height: 400px;
    overflow-y: auto;
    padding: 0.5rem;
  }

  .entry-item {
    background: #f8f9fa;
    padding: 1rem;
    border-radius: 8px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: background 0.2s;
  }

  .entry-item:hover {
    background: #e9ecef;
  }

  .entry-info {
    flex: 1;
  }

  .entry-description {
    font-weight: 600;
    font-size: 1.1rem;
    color: #333;
    margin-bottom: 0.3rem;
  }

  .entry-details {
    font-size: 0.9rem;
    color: #666;
  }

  .delete-btn {
    background: #dc3545;
    color: white;
    border: none;
    border-radius: 6px;
    padding: 0.5rem 1rem;
    cursor: pointer;
    transition: background 0.2s;
    font-size: 0.9rem;
  }

  .delete-btn:hover {
    background: #c82333;
  }

  .entry-form {
    background: #f8f9fa;
    padding: 1.5rem;
    border-radius: 12px;
    border: 2px solid #667eea;
  }

  .entry-form h3 {
    margin-bottom: 1rem;
    color: #333;
  }

  .form-group {
    margin-bottom: 1rem;
  }

  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: #555;
  }

  .form-group input,
  .form-group select {
    width: 100%;
    padding: 0.75rem;
    border: 2px solid #ddd;
    border-radius: 8px;
    font-size: 1rem;
    transition: border-color 0.2s;
  }

  .form-group input:focus,
  .form-group select:focus {
    outline: none;
    border-color: #667eea;
  }

  .form-actions {
    display: flex;
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .btn-primary,
  .btn-secondary {
    flex: 1;
    padding: 0.75rem;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }

  .btn-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
  }

  .btn-secondary {
    background: #6c757d;
    color: white;
  }

  .btn-secondary:hover {
    background: #5a6268;
  }

  .hidden {
    display: none;
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .content-wrapper {
      grid-template-columns: 1fr;
    }

    .chart-section {
      order: 1;
    }

    .entries-section {
      order: 2;
    }
  }

  @media (max-width: 768px) {
    .container {
      padding: 1.5rem;
      border-radius: 15px;
    }

    h1 {
      font-size: 2.2rem;
    }

    .subtitle {
      font-size: 1rem;
    }

    .control-message {
      padding: 1rem;
      font-size: 1.1rem;
    }

    .percentage {
      font-size: 1.4rem;
    }

    .content-wrapper {
      gap: 2rem;
    }

    .chart-section {
      padding: 1rem;
    }

    .entries-header h2 {
      font-size: 1.5rem;
    }

    .add-btn {
      width: 45px;
      height: 45px;
    }

    .entry-form {
      padding: 1rem;
    }

    .form-actions {
      flex-direction: column;
    }
  }

  @media (max-width: 480px) {
    .container {
      padding: 1rem;
    }

    h1 {
      font-size: 1.8rem;
    }

    .subtitle {
      font-size: 0.9rem;
    }

    .control-message {
      padding: 0.8rem;
      font-size: 1rem;
    }

    .percentage {
      font-size: 1.2rem;
    }

    .entries-header h2 {
      font-size: 1.3rem;
    }

    .add-btn {
      width: 40px;
      height: 40px;
    }

    .add-btn svg {
      width: 20px;
      height: 20px;
    }

    .entry-item {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.8rem;
    }

    .delete-btn {
      align-self: flex-end;
    }
  }
</style>

<script>
  interface TimeEntry {
    id: string;
    frequency: string;
    description: string;
    hours: number;
    yearlyHours: number;
    color: string;
  }

  const HOURS_IN_YEAR = 8760; // 365 days * 24 hours

  let entries: TimeEntry[] = [];
  let chart: HTMLCanvasElement;
  let ctx: CanvasRenderingContext2D;

  // Color palette for pie chart
  const colors = [
    '#FF6384', '#36A2EB', '#FFCE56', '#4BC0C0', '#9966FF',
    '#FF9F40', '#FF6384', '#C9CBCF', '#4BC0C0', '#FF9F40'
  ];

  function calculateYearlyHours(frequency: string, hours: number): number {
    switch (frequency) {
      case 'daily':
        return hours * 365;
      case 'weekdays':
        return hours * 260; // 52 weeks * 5 days
      case 'weekends':
        return hours * 104; // 52 weeks * 2 days
      case 'weekly':
        return hours * 52;
      case 'monthly':
        return hours * 12;
      case 'yearly':
        return hours;
      default:
        return 0;
    }
  }

  function getTotalUsedHours(): number {
    return entries.reduce((sum, entry) => sum + entry.yearlyHours, 0);
  }

  function getFreeTimePercentage(): number {
    const usedHours = getTotalUsedHours();
    const freeHours = HOURS_IN_YEAR - usedHours;
    return Math.max(0, (freeHours / HOURS_IN_YEAR) * 100);
  }

  function updateControlMessage() {
    const freePercentage = getFreeTimePercentage();
    const messageEl = document.getElementById('controlMessage');
    const percentageEl = messageEl?.querySelector('.percentage');
    
    if (percentageEl) {
      percentageEl.textContent = `${freePercentage.toFixed(1)}%`;
    }
  }

  function drawPieChart(animate = true) {
    if (!ctx) return;

    const width = chart.width;
    const height = chart.height;
    const centerX = width / 2;
    const centerY = height / 2;
    const radius = Math.min(width, height) / 2 - 20;

    ctx.clearRect(0, 0, width, height);

    const totalUsed = getTotalUsedHours();
    const freeHours = Math.max(0, HOURS_IN_YEAR - totalUsed);
    
    let currentAngle = -Math.PI / 2; // Start at top

    // Draw used time slices
    entries.forEach((entry, index) => {
      const sliceAngle = (entry.yearlyHours / HOURS_IN_YEAR) * 2 * Math.PI;
      
      ctx.beginPath();
      ctx.fillStyle = entry.color;
      ctx.moveTo(centerX, centerY);
      ctx.arc(centerX, centerY, radius, currentAngle, currentAngle + sliceAngle);
      ctx.closePath();
      ctx.fill();

      // Draw slice border
      ctx.strokeStyle = 'white';
      ctx.lineWidth = 2;
      ctx.stroke();

      currentAngle += sliceAngle;
    });

    // Draw free time slice
    if (freeHours > 0) {
      const freeAngle = (freeHours / HOURS_IN_YEAR) * 2 * Math.PI;
      
      ctx.beginPath();
      ctx.fillStyle = '#e0e0e0';
      ctx.moveTo(centerX, centerY);
      ctx.arc(centerX, centerY, radius, currentAngle, currentAngle + freeAngle);
      ctx.closePath();
      ctx.fill();

      ctx.strokeStyle = 'white';
      ctx.lineWidth = 2;
      ctx.stroke();
    }

    // Draw center circle for donut effect
    ctx.beginPath();
    ctx.fillStyle = 'white';
    ctx.arc(centerX, centerY, radius * 0.6, 0, 2 * Math.PI);
    ctx.fill();

    // Draw text in center
    ctx.fillStyle = '#333';
    ctx.font = 'bold 24px sans-serif';
    ctx.textAlign = 'center';
    ctx.textBaseline = 'middle';
    
    const freePercentage = getFreeTimePercentage();
    ctx.fillText(`${freePercentage.toFixed(1)}%`, centerX, centerY - 10);
    
    ctx.font = '14px sans-serif';
    ctx.fillText('Free Time', centerX, centerY + 15);
  }

  function renderEntries() {
    const listEl = document.getElementById('entriesList');
    if (!listEl) return;

    listEl.innerHTML = '';

    if (entries.length === 0) {
      listEl.innerHTML = '<p style="text-align: center; color: #999; padding: 2rem;">No entries yet. Click + to add one!</p>';
      return;
    }

    entries.forEach((entry) => {
      const percentage = (entry.yearlyHours / HOURS_IN_YEAR) * 100;
      const itemEl = document.createElement('div');
      itemEl.className = 'entry-item';
      itemEl.innerHTML = `
        <div style="width: 4px; height: 40px; background: ${entry.color}; border-radius: 2px; margin-right: 1rem;"></div>
        <div class="entry-info">
          <div class="entry-description">${entry.description}</div>
          <div class="entry-details">
            ${entry.hours}h ${entry.frequency} • ${entry.yearlyHours.toFixed(0)}h/year • ${percentage.toFixed(1)}%
          </div>
        </div>
        <button class="delete-btn" data-id="${entry.id}">Delete</button>
      `;

      const deleteBtn = itemEl.querySelector('.delete-btn');
      deleteBtn?.addEventListener('click', () => {
        entries = entries.filter(e => e.id !== entry.id);
        saveToLocalStorage();
        renderEntries();
        drawPieChart();
        updateControlMessage();
      });

      listEl.appendChild(itemEl);
    });
  }

  function saveToLocalStorage() {
    localStorage.setItem('netlife-entries', JSON.stringify(entries));
  }

  function loadFromLocalStorage() {
    const stored = localStorage.getItem('netlife-entries');
    if (stored) {
      try {
        entries = JSON.parse(stored);
      } catch (e) {
        entries = [];
      }
    }
  }

  function initializeApp() {
    chart = document.getElementById('pieChart') as HTMLCanvasElement;
    ctx = chart.getContext('2d')!;

    // Make canvas responsive
    function resizeCanvas() {
      const container = chart.parentElement;
      if (container) {
        const size = Math.min(container.clientWidth - 40, 400);
        chart.width = size;
        chart.height = size;
        drawPieChart(false);
      }
    }

    window.addEventListener('resize', resizeCanvas);
    resizeCanvas();

    loadFromLocalStorage();
    renderEntries();
    drawPieChart(false);
    updateControlMessage();

    // Add entry button
    const addBtn = document.getElementById('addEntryBtn');
    const entryForm = document.getElementById('entryForm');
    const saveBtn = document.getElementById('saveBtn');
    const cancelBtn = document.getElementById('cancelBtn');

    addBtn?.addEventListener('click', () => {
      entryForm?.classList.remove('hidden');
      addBtn.style.display = 'none';
    });

    cancelBtn?.addEventListener('click', () => {
      entryForm?.classList.add('hidden');
      addBtn!.style.display = 'flex';
      
      // Reset form
      (document.getElementById('frequency') as HTMLSelectElement).value = 'daily';
      (document.getElementById('description') as HTMLInputElement).value = '';
      (document.getElementById('hours') as HTMLInputElement).value = '';
    });

    saveBtn?.addEventListener('click', () => {
      const frequency = (document.getElementById('frequency') as HTMLSelectElement).value;
      const description = (document.getElementById('description') as HTMLInputElement).value.trim();
      const hours = parseFloat((document.getElementById('hours') as HTMLInputElement).value);

      if (!description || !hours || hours <= 0) {
        alert('Please fill in all fields with valid values');
        return;
      }

      const yearlyHours = calculateYearlyHours(frequency, hours);
      
      if (getTotalUsedHours() + yearlyHours > HOURS_IN_YEAR) {
        alert('This would exceed the total hours in a year (8760 hours). Please adjust your entries.');
        return;
      }

      const newEntry: TimeEntry = {
        id: Date.now().toString(),
        frequency,
        description,
        hours,
        yearlyHours,
        color: colors[entries.length % colors.length]
      };

      entries.push(newEntry);
      saveToLocalStorage();
      
      // Reset form and hide
      entryForm?.classList.add('hidden');
      addBtn!.style.display = 'flex';
      (document.getElementById('frequency') as HTMLSelectElement).value = 'daily';
      (document.getElementById('description') as HTMLInputElement).value = '';
      (document.getElementById('hours') as HTMLInputElement).value = '';

      renderEntries();
      animateChartUpdate();
      updateControlMessage();
    });
  }

  function animateChartUpdate() {
    let frame = 0;
    const totalFrames = 30;

    function animate() {
      frame++;
      if (frame <= totalFrames) {
        drawPieChart();
        requestAnimationFrame(animate);
      }
    }

    animate();
  }

  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initializeApp);
  } else {
    initializeApp();
  }
</script>
